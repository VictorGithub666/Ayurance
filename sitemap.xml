<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9
        http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd">

  <!-- Homepage - this is currently the only real, independently-crawlable URL on the site -->
  <url>
    <loc>https://ayurance.com/</loc>
    <lastmod>2026-07-14</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.00</priority>
  </url>

  <!--
    Note: #about, #therapies, #conditions, #contact, #map-section were removed.
    URL fragments (#...) all point to the SAME page (https://ayurance.com/) and are
    not separately crawlable or indexable — Google ignores everything after the "#".
    Listing them as separate <url> entries doesn't help SEO and can look like
    low-quality sitemap padding. If/when you build standalone pages (e.g. a
    dedicated /therapies/ or /locations/uk/ page), add those real URLs here.
  -->

</urlset>
